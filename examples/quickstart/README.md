# Quickstart

Minimal local setup for the suite.

## Files

- `compose.yml`: local service composition example
- `composer.yml`: minimal composer backend config
- `pipelines/hello-world.yaml`: starter Forge pipeline

## Run

```bash
docker compose -f compose.yml up --build
```
