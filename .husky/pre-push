#!/usr/bin/env sh
set -eu

BRANCH="$(git rev-parse --abbrev-ref HEAD)"

case "$BRANCH" in
  feat/*|fix/*|perf/*)
    node scripts/verify-toolchain.mjs --repository
    node scripts/release-preflight.mjs
    ;;
esac
