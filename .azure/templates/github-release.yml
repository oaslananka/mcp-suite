steps:
  - download: current
    artifact: lab-macos

  - download: current
    artifact: lab-windows

  - download: current
    artifact: lab-linux

  - script: node scripts/create-github-release.sh
    displayName: Create GitHub release and upload assets
    env:
      GITHUB_TOKEN: $(GITHUB_TOKEN)
      GITHUB_REPO: $(GITHUB_REPO)
      RELEASE_VERSION: $(RELEASE_VERSION)
